import 'package:flutter/material.dart';

class CampicPage extends StatelessWidget {
  const CampicPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        "Campic Page",
        style: TextStyle(color: Colors.white),
      ),
    );
  }
}
